apiVersion: stunner.l7mp.io/v1
kind: UDPRoute
metadata:
  name: livekit-media
  namespace: livekit
spec:
  parentRefs:
    - name: udp-gateway
      namespace: stunner
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: livekit-server
