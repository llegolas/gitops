apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: oidc-dashboard
  namespace: envoy-keycloak-poc
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: poc-dashboard-route
  oidc:
    provider:
      issuer: https://keycloak.CLUSTER_DOMAIN/realms/poc
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: keycloak-external
          port: 443
          weight: 1
    clientID: envoy-gateway-poc
    clientSecret:
      name: keycloak-oidc-client-secret
    redirectURL: https://poc.CLUSTER_DOMAIN/oauth2/callback-dashboard
    logoutPath: /oauth2/logout-dashboard
    cookieDomain: CLUSTER_DOMAIN
    cookieNames:
      accessToken: keycloak-access-token
    scopes:
      - openid
      - groups
    defaultTokenTTL: 3600s
    forwardAccessToken: true
  jwt:
    providers:
      - name: keycloak
        issuer: https://keycloak.CLUSTER_DOMAIN/realms/poc
        remoteJWKS:
          uri: https://keycloak.CLUSTER_DOMAIN/realms/poc/protocol/openid-connect/certs
          cacheDuration: 300s
          backendRefs:
            - group: gateway.envoyproxy.io
              kind: Backend
              name: keycloak-external
              port: 443
              weight: 1
        extractFrom:
          cookies:
            - keycloak-access-token
        claimToHeaders:
          - claim: preferred_username
            header: x-user
          - claim: email
            header: x-email
  authorization:
    defaultAction: Deny
    rules:
      - name: users
        action: Allow
        principal:
          jwt:
            provider: keycloak
            claims:
              - name: groups
                valueType: StringArray
                values:
                  - users
