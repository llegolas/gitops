apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: poc-dashboard-route
  namespace: envoy-keycloak-poc
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      namespace: envoy-gateway-system
      name: GATEWAY_NAME
      sectionName: https-terminated
  hostnames:
    - poc.CLUSTER_DOMAIN
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /dashboard
      backendRefs:
        - group: ""
          kind: Service
          name: dashboard-service
          port: 8080
          weight: 1
    - matches:
        - path:
            type: Exact
            value: /oauth2/callback-dashboard
        - path:
            type: Exact
            value: /oauth2/logout-dashboard
      backendRefs:
        - group: ""
          kind: Service
          name: dashboard-service
          port: 8080
          weight: 1
